services:
  # Python Backend
  - type: web
    name: trading-platform-python
    plan: free
    env: python
    rootDir: backend-python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8083
